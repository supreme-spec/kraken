import { spawn } from 'child_process'; import { fileURLToPath } from 'url'; import { dirname, join } from 'path'; const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename); const isWin = process.platform === 'win32'; const py = isWin ? join(__dirname, 'venv', 'Scripts', 'python.exe') : 'venv/bin/python'; const python = (await import('fs').then(fs => fs.existsSync(py) ? py : 'python')) || 'python'; spawn(python, ['face_server.py'], { stdio: 'inherit', cwd: __dirname });
